# FluidSimJourney

Interactive notes and code for fluid-simulation experiments.

This repository contains an mdBook-based book (`book/`) and a `src/` folder with scripts and source materials.

To run locally:
- Open `book/index.html` in a browser for a built version, or use `mdbook serve` if you have the mdbook tool installed.

License: see `LICENSE` or project files.
